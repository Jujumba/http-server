#ifndef HTTP_SERVER_STRINGS_H
#define HTTP_SERVER_STRINGS_H

#include <stddef.h>
#include "panic.h"

void concat_strings(char *restrict, char *restrict, size_t *restrict, size_t *restrict);

#endif //HTTP_SERVER_STRINGS_H